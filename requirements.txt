# LedgerMind - GST Compliance Assistant
# Requirements

# Core dependencies (already have these)
pdfplumber>=0.10.0
chromadb>=0.4.18
sentence-transformers>=2.2.0

# For hybrid search (semantic + keyword)
rank-bm25>=0.2.2

# For sentence-aware chunking
nltk>=3.8.1

# New for LLM integration
requests>=2.31.0  # For Ollama API calls

# For NLI-based faithfulness calculation
transformers>=4.30.0  # HuggingFace transformers for NLI model
torch>=2.0.0  # PyTorch backend for transformers

# Optional: for better error messages
colorama>=0.4.6
